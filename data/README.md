# Dataset

The raw dataset used for this project is not included in the Git repository because of its file size.

Place the dataset at:

data/raw/Dataset.csv

The dataset is required for:

- Exploratory Data Analysis
- Data preprocessing
- Model retraining
- Reproducing the experiments

The trained model files required to run the Streamlit application are already stored in:

models/